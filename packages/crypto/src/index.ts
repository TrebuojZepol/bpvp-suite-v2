/**
 * Shared cryptographic helpers (wrappers around audited primitives).
 * TODO: integrate secp256k1, PSBT, taproot helpers as needed — no protocol logic here yet.
 */

export const cryptoPackagePlaceholder = true;
