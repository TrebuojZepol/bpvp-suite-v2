# Protobuf definitions

Place `.proto` files and generation scripts here.

## TODO

- Define service APIs between dashboard/wallet and backend services.
- Add `buf` or `protoc` codegen into `packages/shared` TypeScript and Go outputs.
- Version schemas and enforce backward-compatible changes.
