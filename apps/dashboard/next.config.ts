import type { NextConfig } from "next";

// TODO: configure headers, redirects, and env validation for production.

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
